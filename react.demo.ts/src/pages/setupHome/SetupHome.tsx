import * as React from 'react';
import './SetupHome.scss';
import { Layout } from 'antd';

export default class SetupHome extends React.Component {
    public render() {
        return <Layout style={{ height: '100%' }}>
            HomePage
        </Layout>
    }
}